---
phase: 04-economic-tiles
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - server.ts
  - tests/tiles-econ.test.ts
autonomous: true
requirements: [ECON-01, ECON-02, ECON-03, ECON-04, ECON-05, ECON-06, ECON-07, ECON-08, ECON-09, ECON-10]

must_haves:
  truths:
    - "tests/tiles-econ.test.ts exists with describe blocks for all 10 ECON requirements"
    - "All 10 ECON describe blocks have failing test stubs (MISSING implementations throw or use placeholder)"
    - "Player interface in server.ts has hasPonziFlag boolean field"
    - "Player factory (createPlayer) initializes hasPonziFlag: false"
    - "BOARD_TILES positions 3, 7, 13, 15, 17, 23, 27, 33-39 are updated to economic tile types"
    - "npm test -- tiles-econ.test.ts runs without import errors (stubs may fail, but file loads)"
  artifacts:
    - path: "tests/tiles-econ.test.ts"
      provides: "Test scaffold for ECON-01 through ECON-10"
      exports: []
    - path: "server.ts"
      provides: "Player.hasPonziFlag field + updated BOARD_TILES"
      contains: "hasPonziFlag"
  key_links:
    - from: "tests/tiles-econ.test.ts"
      to: "server.ts"
      via: "import { createPlayer, createGameRoom, dispatchTile, ... } from '../server'"
      pattern: "from '../server'"
---

<objective>
Create the test scaffold and update server.ts infrastructure that all Phase 4 implementation plans depend on.

Purpose: VALIDATION.md requires Wave 0 to exist before any ECON tile implementation. Without the test file, subsequent plans have no `<automated>` verify targets. Without `hasPonziFlag` on Player, ECON-09 (Ponzi) has no field to write to. Without BOARD_TILES economic tile types, dispatchTile() cannot route to ECON cases.

Output:
- tests/tiles-econ.test.ts with 10 describe blocks (one per ECON requirement), each with 2+ stub tests
- server.ts Player interface gains hasPonziFlag: boolean
- server.ts createPlayer() initializes hasPonziFlag: false
- server.ts BOARD_TILES: TBD positions replaced with economic tile types
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-economic-tiles/04-RESEARCH.md
@.planning/phases/04-economic-tiles/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hasPonziFlag to Player interface and createPlayer factory</name>
  <files>server.ts</files>
  <read_first>
    - server.ts (lines 18–53 for Player interface; lines 219–248 for createPlayer factory)
  </read_first>
  <behavior>
    - createPlayer('x', 'test').hasPonziFlag === false
    - Player interface accepts hasPonziFlag: boolean without TypeScript error
  </behavior>
  <action>
In server.ts, make two targeted edits:

1. In the `Player` interface (around line 18), add `hasPonziFlag: boolean;` after the `hasStudentLoans: boolean;` line. The exact addition:
```typescript
  hasStudentLoans: boolean;
  hasPonziFlag: boolean;    // ← ADD THIS LINE
  // Character portrait overlays
```

2. In the `createPlayer` factory function (around line 219), add `hasPonziFlag: false,` after the `hasStudentLoans: false,` line:
```typescript
    hasStudentLoans: false,
    hasPonziFlag: false,    // ← ADD THIS LINE
    hasWeddingRing: false,
```

No other changes to server.ts in this task.
  </action>
  <verify>
    <automated>cd /Users/ringochui/Projects/gig && npm test -- --testPathPattern="game-loop|lobby|room|rate|state|sync|disconnect|heartbeat" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - server.ts contains line matching `hasPonziFlag: boolean`
    - server.ts contains line matching `hasPonziFlag: false`
    - `npm test` (full suite) exits 0 — no TypeScript compile errors, no regressions
  </acceptance_criteria>
  <done>hasPonziFlag appears in Player interface and createPlayer initializer; all existing tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Update BOARD_TILES to add economic tile types at TBD positions</name>
  <files>server.ts</files>
  <read_first>
    - server.ts (lines 174–215 for BOARD_TILES constant — all 40 entries)
  </read_first>
  <action>
In server.ts, replace the 10 `TBD` tiles in BOARD_TILES with economic tile types as follows. The current TBD positions and their new values:

```
Position 3:  { type: 'TBD', name: 'TBD...' }
  → { type: 'SPORTS_BETTING',     name: 'Sports Betting' }

Position 7:  { type: 'TBD', name: 'TBD...' }
  → { type: 'INVESTMENT_POOL',    name: 'Investment Pool' }

Position 13: { type: 'TBD', name: 'TBD...' }
  → { type: 'COVID_STIMULUS',     name: 'COVID Stimulus Check' }

Position 15: { type: 'TBD', name: 'TBD...' }
  → { type: 'TAX_AUDIT',          name: 'Tax Audit' }

Position 17: { type: 'TBD', name: 'TBD...' }
  → { type: 'SCRATCH_TICKET',     name: 'Scratch Ticket' }

Position 23: { type: 'TBD', name: 'TBD...' }
  → { type: 'CRYPTO',             name: 'Crypto' }

Position 27: { type: 'TBD', name: 'TBD...' }
  → { type: 'NEPOTISM',           name: 'Nepotism' }

Position 33: { type: 'TBD', name: 'TBD...' }
  → { type: 'UNION_STRIKE',       name: 'Union Strike' }

Position 34: { type: 'TBD', name: 'TBD...' }
  → { type: 'PONZI_SCHEME',       name: 'Ponzi Scheme' }

Position 35: { type: 'TBD', name: 'TBD...' }
  → { type: 'STUDENT_LOAN_PAYMENT', name: 'Student Loan Payment' }
```

Leave positions 36, 37, 38, 39 as `{ type: 'TBD', name: 'TBD...' }` — those are reserved for future phases.

BOARD_TILES must still have exactly 40 entries total.
  </action>
  <verify>
    <automated>cd /Users/ringochui/Projects/gig && npm test -- --testPathPattern="game-loop" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - server.ts contains `type: 'SPORTS_BETTING'`
    - server.ts contains `type: 'INVESTMENT_POOL'`
    - server.ts contains `type: 'COVID_STIMULUS'`
    - server.ts contains `type: 'TAX_AUDIT'`
    - server.ts contains `type: 'SCRATCH_TICKET'`
    - server.ts contains `type: 'CRYPTO'`
    - server.ts contains `type: 'NEPOTISM'`
    - server.ts contains `type: 'UNION_STRIKE'`
    - server.ts contains `type: 'PONZI_SCHEME'`
    - server.ts contains `type: 'STUDENT_LOAN_PAYMENT'`
    - game-loop.test.ts assertion `BOARD_TILES has exactly 40 entries` still passes
    - game-loop.test.ts assertion `every BOARD_TILES entry has type and name strings` still passes
  </acceptance_criteria>
  <done>All 10 economic tile types present in BOARD_TILES; board is still 40 tiles; game-loop tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/tiles-econ.test.ts with failing stubs for ECON-01..10</name>
  <files>tests/tiles-econ.test.ts</files>
  <read_first>
    - tests/game-loop.test.ts (lines 1–38 for import block and createMockGameRoom fixture pattern)
    - .planning/phases/04-economic-tiles/04-RESEARCH.md (lines 86–120 for tile handler patterns)
    - .planning/phases/04-economic-tiles/04-VALIDATION.md (lines 39–52 for per-task test map)
  </read_first>
  <action>
Create `tests/tiles-econ.test.ts`. Copy the import and fixture pattern from game-loop.test.ts exactly. Add a `createMockRoom3Players` helper (3-player variant used by Union Strike and Ponzi tests). Structure:

```typescript
'use strict';

import {
  createPlayer,
  createGameRoom,
  GAME_PHASES,
  TURN_PHASES,
  STARTING_MONEY,
  BOARD_TILES,
  advanceTurn,
  dispatchTile,
} from '../server';

// ── Fixture helpers ────────────────────────────────────────────────────────

function createMockRoom() {
  const room = createGameRoom('TEST', 'socket-a');
  const alice = createPlayer('socket-a', 'Alice', true);
  const bob   = createPlayer('socket-b', 'Bob', false);
  room.players.set('socket-a', alice);
  room.players.set('socket-b', bob);
  room.turnOrder = ['socket-a', 'socket-b'];
  room.currentTurnIndex = 0;
  room.gamePhase = GAME_PHASES.PLAYING;
  room.turnPhase = TURN_PHASES.WAITING_FOR_ROLL;
  return room;
}

function createMockRoom3Players() {
  const room = createMockRoom();
  const carol = createPlayer('socket-c', 'Carol', false);
  room.players.set('socket-c', carol);
  room.turnOrder.push('socket-c');
  return room;
}

afterAll((done) => {
  require('../server').httpServer.close(done);
});

// ── ECON-01: Sports Betting ────────────────────────────────────────────────
describe('ECON-01 Sports Betting', () => {
  it('TODO: win on roll=1 gives 6× bet', () => {
    expect(true).toBe(false); // STUB — implement in plan 01
  });
  it('TODO: loss on roll!=1 deducts entire bet', () => {
    expect(true).toBe(false); // STUB — implement in plan 01
  });
  it('TODO: money never goes below 0 on loss', () => {
    expect(true).toBe(false); // STUB — implement in plan 01
  });
});

// ── ECON-02: Investment Pool ───────────────────────────────────────────────
describe('ECON-02 Investment Pool', () => {
  it('TODO: roll=1 wins entire pool and resets pool to 0', () => {
    expect(true).toBe(false); // STUB — implement in plan 02
  });
  it('TODO: roll!=1 deducts $500 from player and adds to pool', () => {
    expect(true).toBe(false); // STUB — implement in plan 02
  });
  it('TODO: pool persists across multiple player landings', () => {
    expect(true).toBe(false); // STUB — implement in plan 02
  });
});

// ── ECON-03: COVID Stimulus ────────────────────────────────────────────────
describe('ECON-03 COVID Stimulus', () => {
  it('TODO: all players receive $1,400 flat', () => {
    expect(true).toBe(false); // STUB — implement in plan 01
  });
  it('TODO: stimulus applies to all players including current player', () => {
    expect(true).toBe(false); // STUB — implement in plan 01
  });
});

// ── ECON-04: Tax Audit ─────────────────────────────────────────────────────
describe('ECON-04 Tax Audit', () => {
  it('TODO: deduction = Math.floor(money * (roll * 5) / 100)', () => {
    expect(true).toBe(false); // STUB — implement in plan 01
  });
  it('TODO: roll=6 deducts 30% of current money', () => {
    expect(true).toBe(false); // STUB — implement in plan 01
  });
  it('TODO: result never goes below 0', () => {
    expect(true).toBe(false); // STUB — implement in plan 01
  });
});

// ── ECON-05: Scratch Ticket ────────────────────────────────────────────────
describe('ECON-05 Scratch Ticket', () => {
  it('TODO: roll=1 results in net +$1,800 (win $2,000 minus $200 cost)', () => {
    expect(true).toBe(false); // STUB — implement in plan 01
  });
  it('TODO: roll=2 or roll=3 results in net -$200 (break even after paying $200)', () => {
    expect(true).toBe(false); // STUB — implement in plan 01
  });
  it('TODO: roll=4 through roll=6 results in net -$400 (lose $200 cost + lose $200)', () => {
    expect(true).toBe(false); // STUB — implement in plan 01
  });
  it('TODO: money can go negative on scratch ticket (no floor)', () => {
    expect(true).toBe(false); // STUB — implement in plan 01
  });
});

// ── ECON-06: Crypto ────────────────────────────────────────────────────────
describe('ECON-06 Crypto', () => {
  it('TODO: first landing with investment amount stores in cryptoInvestments and deducts money', () => {
    expect(true).toBe(false); // STUB — implement in plan 02
  });
  it('TODO: second landing with roll=1 or roll=2 returns 3× investment', () => {
    expect(true).toBe(false); // STUB — implement in plan 02
  });
  it('TODO: second landing with roll=3 or roll=4 returns original investment (break even)', () => {
    expect(true).toBe(false); // STUB — implement in plan 02
  });
  it('TODO: second landing with roll=5 or roll=6 returns 0 (worthless)', () => {
    expect(true).toBe(false); // STUB — implement in plan 02
  });
  it('TODO: cryptoInvestments reset to 0 after payout', () => {
    expect(true).toBe(false); // STUB — implement in plan 02
  });
});

// ── ECON-07: Nepotism ─────────────────────────────────────────────────────
describe('ECON-07 Nepotism', () => {
  it('TODO: current player gains $1,000', () => {
    expect(true).toBe(false); // STUB — implement in plan 03
  });
  it('TODO: chosen beneficiary gains $500', () => {
    expect(true).toBe(false); // STUB — implement in plan 03
  });
  it('TODO: invalid beneficiary (self or nonexistent) is rejected', () => {
    expect(true).toBe(false); // STUB — implement in plan 03
  });
});

// ── ECON-08: Union Strike ─────────────────────────────────────────────────
describe('ECON-08 Union Strike', () => {
  it('TODO: total money is redistributed equally to all players', () => {
    expect(true).toBe(false); // STUB — implement in plan 03
  });
  it('TODO: each player receives Math.floor(total / playerCount)', () => {
    expect(true).toBe(false); // STUB — implement in plan 03
  });
  it('TODO: all updates are applied before broadcast (atomic)', () => {
    expect(true).toBe(false); // STUB — implement in plan 03
  });
});

// ── ECON-09: Ponzi Scheme ─────────────────────────────────────────────────
describe('ECON-09 Ponzi Scheme', () => {
  it('TODO: steals $1,000 from each other player', () => {
    expect(true).toBe(false); // STUB — implement in plan 04
  });
  it('TODO: sets hasPonziFlag to true on attacker', () => {
    expect(true).toBe(false); // STUB — implement in plan 04
  });
  it('TODO: repays exactly 2× stolen amount to each victim on next money tile landing', () => {
    expect(true).toBe(false); // STUB — implement in plan 04
  });
  it('TODO: hasPonziFlag cleared after repayment', () => {
    expect(true).toBe(false); // STUB — implement in plan 04
  });
  it('TODO: Ponzi player cannot steal more than victim has', () => {
    expect(true).toBe(false); // STUB — implement in plan 04
  });
});

// ── ECON-10: Student Loan Payment ─────────────────────────────────────────
describe('ECON-10 Student Loan Payment', () => {
  it('TODO: player with hasStudentLoans=true loses $1,000 on landing', () => {
    expect(true).toBe(false); // STUB — implement in plan 04
  });
  it('TODO: player with hasStudentLoans=false is unaffected on landing', () => {
    expect(true).toBe(false); // STUB — implement in plan 04
  });
  it('TODO: every landing re-deducts (no one-time immunity)', () => {
    expect(true).toBe(false); // STUB — implement in plan 04
  });
});
```

The file must import from `'../server'` exactly as shown. All stubs use `expect(true).toBe(false)` so they fail red (RED state confirmed before GREEN in later plans).
  </action>
  <verify>
    <automated>cd /Users/ringochui/Projects/gig && npm test -- tiles-econ.test.ts 2>&1 | grep -E "Tests:|Test Suites:|FAIL|PASS|Cannot find"</automated>
  </verify>
  <acceptance_criteria>
    - tests/tiles-econ.test.ts exists
    - `npm test -- tiles-econ.test.ts` output shows "FAIL tests/tiles-econ.test.ts" (stubs fail red, NOT a module import error)
    - Output contains "Tests:" line showing multiple failing tests (not 0)
    - Output does NOT contain "Cannot find module '../server'"
    - File contains `describe('ECON-01` through `describe('ECON-10`
    - File contains `afterAll((done) => { require('../server').httpServer.close(done); })`
  </acceptance_criteria>
  <done>Test scaffold exists, imports cleanly, stubs fail red confirming RED state; 10 describe blocks present covering ECON-01..10</done>
</task>

</tasks>

<verification>
Full suite check after Wave 0:
- `npm test` (full suite) must pass all existing tests (no regressions from Player interface change)
- `npm test -- tiles-econ.test.ts` must show FAIL with failing stubs (not import errors)
- `grep 'hasPonziFlag' server.ts` must return 2+ lines (interface field + factory init)
- `grep 'SPORTS_BETTING\|INVESTMENT_POOL\|COVID_STIMULUS\|TAX_AUDIT\|SCRATCH_TICKET\|CRYPTO\|NEPOTISM\|UNION_STRIKE\|PONZI_SCHEME\|STUDENT_LOAN_PAYMENT' server.ts | wc -l` must be >= 10
</verification>

<success_criteria>
- Player.hasPonziFlag: boolean exists in server.ts interface and factory
- All 10 economic tile type strings exist in BOARD_TILES
- tests/tiles-econ.test.ts loads without import errors
- All stubs fail red (confirming TDD RED state for plans 01-04)
- Full test suite passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-economic-tiles/04-00-SUMMARY.md`
</output>
